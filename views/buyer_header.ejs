<!DOCTYPE html>
<html class="no-js">
    <head>
        <meta charset="utf-8">
        <title>.</title>
        <link rel="icon" type="image/png" href="/imgs/logo.png"/>
        <link rel="stylesheet" href="/css/styles.css">
        
        <script>
            var asc1 = 1,
                asc2 = 1,
                asc3 = 1;

            function sort_table(tbody, col, asc) {
                var rows = tbody.rows,
                    rlen = rows.length,
                    arr = new Array(),
                    i, j, cells, clen;
                // fill the array with values from the table
                for (i = 0; i < rlen; i++) {
                    cells = rows[i].cells;
                    clen = cells.length;
                    arr[i] = new Array();
                    for (j = 0; j < clen; j++) {
                        arr[i][j] = cells[j].innerHTML;
                    }
                }
                // sort the array by the specified column number (col) and order (asc)
                arr.sort(function (a, b) {
                    return (a[col] == b[col]) ? 0 : ((a[col] > b[col]) ? asc : -1 * asc);
                });
                // replace existing rows with new rows created from the sorted array
                for (i = 0; i < rlen; i++) {
                    rows[i].innerHTML = "<td>" + arr[i].join("</td><td>") + "</td>";
                }
            }
        </script>
    </head>
    <body>
        <div class="top-navbar">
            
            <div class="top-navbar-kid">
                <p><a href="/home"><img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRhnnn2_DFSuDR4Med9y4_CT9bNa5r6YcynCA&usqp=CAU" style="border-radius:100%;height:40px;width:40px;"></a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/home">Home</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/profile">Profile</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/whishlist">Whishlist</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/orders">Orders</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/history">History</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/cart">cart</a></p>
            </div>
            <div class="top-navbar-kid">
                <p><a href="/seller-home-page">Sell</a></p>
            </div>
            <% if (logout) {%>
                <div class="top-navbar-kid logout-btn">
                    <form action="/logout" method="POST">
                        <button>LOGOUT</button>
                    </form>                    
                </div>
            <%}%>

        </div>